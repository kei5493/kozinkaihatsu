<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kozinkaihatsu.app.repository.common.FlowerMapper">
    <!-- 登録処理 -->
    <select id="selectMaxId" resultType="int"> 
    SELECT COALESCE(MAX(id), 0) FROM kozinkaihatsu.flower
    </select>
   <insert id="insertFlower" parameterType="com.kozinkaihatsu.app.Record.FlowersListRecord">
    INSERT INTO kozinkaihatsu.flower (
        id,
        flower_name_id,
        flower_name,        
        color_id,
        color_name,        
        image_file_name
    ) VALUES (
        #{id},
        #{flowerNameId},
        #{flowerName},      
        #{colorId},
        #{colorName},      
        #{imageFileName}
    )
    </insert>

    <!-- 色マスタ取得 -->
    <select id="selectAllColors" resultType="com.kozinkaihatsu.app.Record.FlowerColorRecord">
        SELECT
            color_id AS colorId,
            color_name AS colorName
        FROM
            kozinkaihatsu.flower_color
        ORDER BY color_id
    </select>

</mapper>
